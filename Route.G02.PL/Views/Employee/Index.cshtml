@model IEnumerable<Employee>

@{
    ViewData["Title"] = "Index";


}




<h1>All Employees</h1>
<br/>
<br/>

<a asp-asp-controller="Employee" asp-action="Create" class="btn btn-primary">Create New Employee</a>

<br/>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <form asp-action="Index" class="row g-3">
                <div class="col-9">
                    <input type="text" class="form-control" name="searchInp" id="searchInp" placeholder="Search By Name" />
                </div>
                <div class="col-3">
                    <button type="submit" class="btn btn-success w-100">Search</button>
                </div>
            </form>
        </div>
    </div>
</div>



@if(Model.Count()> 0)
{
    <table class="mt-4 table table-striped table-hover"> 
        <thead> 
            <tr>
                <th>@Html.DisplayNameFor(D => D.Name)</th>
                <th>@Html.DisplayNameFor(D => D.Age)</th>
                <th>@Html.DisplayNameFor(D => D.Salary)</th>
                <th>@Html.DisplayNameFor(D => D.Department)</th>
                <th>@Html.DisplayNameFor(D => D.IsActive)</th>
                <th>@Html.DisplayNameFor(D => D.Gender)</th>
                <th>@Html.DisplayNameFor(D => D.Email)</th>
                @*<th>@Html.DisplayNameFor(D => D.PhoneNumber)</th>*@
                @*<th>@Html.DisplayNameFor(D => D.EmployeeType)</th>>*@
                @*<th>@Html.DisplayNameFor(D => D.HiringDate)</th>*@
                <th>Details</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>
        </thead>   
        <tbody>
                @foreach (var employee in Model)
                {
                    <tr>
                    <td>@employee.Name</td>
                    <td>@employee.Age</td>
                    <td>@Html.DisplayFor(M => employee.Salary)</td>
                    <td>@employee.Department?.Name</td>
                    <td>@Html.DisplayFor(M => employee.IsActive)</td>
                    <td>@employee.Gender</td>
                    <td>@Html.DisplayFor(M => employee.Email)</td>
                        
                    <partial name="_ButtonPartial" model="@employee.Id"/>
                    </tr>
                }
            </tbody>
    </table>
}
else
{
    <div class="mt-4 alert alert-warning">
        <h3>Ther is No Employee :(</h3>
    </div>
}


